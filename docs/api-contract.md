# API-контракт

Этот документ — **источник истины** для контракта между бэкендом и фронтендом.
Обе команды (обе части монорепы) ориентируются на этот файл.

## Статус

🚧 **Пустой шаблон.** Будет заполняться по мере реализации эндпоинтов на Этапе 4.

## Принцип работы

1. Перед реализацией нового эндпоинта — записать его контракт здесь
2. Бэкенд реализует согласно записанному контракту
3. Фронтенд пишет клиент согласно записанному контракту
4. Любое изменение контракта — это **совместное** решение, затрагивающее
   обе стороны. Обсуждать до изменения, не после.
5. OpenAPI-спецификация (`/v3/api-docs` на бэке) должна **всегда** соответствовать
   этому документу. Если расходятся — это bug.

## Связанные документы

- [`backend/docs/api-design.md`](../backend/docs/api-design.md) — правила
  дизайна REST API (формат, пагинация, ошибки, версионирование). Это
  "как устроено", а не "что именно".
- `frontend/docs/api-client.md` (появится позже) — как фронт использует API.

## Базовые решения

- **Base URL:** `/api/v1`
- **Формат:** JSON, UTF-8, `camelCase` поля
- **Даты:** ISO 8601 с таймзоной (`2026-04-13T10:30:00Z`)
- **ID:** UUID v4 как строки
- **Ошибки:** RFC 7807 Problem Details
- **Аутентификация:** пока нет, будет Bearer JWT (Этап 6+)

## Эндпоинты

### Темы (Topics)

_Появятся после Этапа 4._

<!--
Шаблон для описания эндпоинта:

#### POST /api/v1/topics
**Назначение:** создать новую тему с корневым вопросом

**Запрос:**
```json
{
  "title": "string, 1-200 символов",
  "description": "string, optional, до 2000 символов",
  "rootQuestion": "string, 1-10000 символов"
}
```

**Ответ (201 Created):**
```json
{
  "id": "uuid",
  "title": "string",
  "description": "string|null",
  "rootNode": { ... NodeResponse ... },
  "createdAt": "iso8601"
}
```

**Ошибки:**
- `400` — невалидные поля
- `422` — бизнес-правило нарушено (какое — указать)

**Примечания:**
- Любые специфичные нюансы поведения
-->

### Узлы (Nodes)

_Появятся после Этапа 4._

### Связи (Edges)

_Появятся после Этапа 4._

### Источники (Sources)

_Появятся после Этапа 5._

### Авторитеты (Authorities)

_Появятся после Этапа 5._

## Общие типы ответов

### NodeSummary
```json
{
  "id": "uuid",
  "nodeType": "QUESTION|CLAIM|ARGUMENT|EVIDENCE",
  "contentPreview": "string, первые ~100 символов",
  "status": "STANDING|DISPUTED|REFUTED|UNVERIFIED"
}
```

### UserSummary
```json
{
  "id": "uuid",
  "username": "string"
}
```

### ErrorResponse (Problem Details)
```json
{
  "type": "https://argumentmap.example/errors/<code>",
  "title": "Короткое название",
  "status": 400,
  "detail": "Подробное описание",
  "instance": "/api/v1/..."
}
```

Для ошибок валидации дополнительно:
```json
{
  ...,
  "errors": [
    { "field": "content", "message": "не должно быть пустым" }
  ]
}
```

## История изменений контракта

| Дата | Версия API | Что изменилось | Причина |
|------|------------|----------------|---------|
| —    | v1         | (первая версия) | — |
