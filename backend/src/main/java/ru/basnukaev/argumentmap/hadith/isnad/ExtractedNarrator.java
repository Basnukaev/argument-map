package ru.basnukaev.argumentmap.hadith.isnad;

/**
 * Один передатчик иснада, извлечённый LLM из матна арабского хадиса
 * (ADR-059). Биография не извлекается — только имя как оно стоит в
 * матне и формула передачи (тахаммуль), по которой ЭТОТ передатчик
 * получил хадис от СЛЕДУЮЩЕГО передатчика в сторону Пророка ﷺ.
 *
 * @param name         имя передатчика на арабском как в матне
 *                     (например {@code "مالك"})
 * @param transmission формула передачи (حدثنا / أخبرنا / سمعت / عن /
 *                     أنّ / عن النبي …); может быть null если LLM не
 *                     распознал
 */
public record ExtractedNarrator(String name, String transmission) {
}
