<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        ADR-040: расширение users под Spring Security + JWT.
        Базовая таблица создана миграцией 02 (id/username/email/created_at).
        Добавляем поля для password-based auth + role + enabled flag.

        password_hash NULLABLE на время transition: существующие dev
        пользователи без пароля остаются работать через X-User-Id
        fallback фильтр (dev profile). Новые user'ы через POST /auth/register
        пишутся с NOT NULL hash. После Этапа 21.b - сделать NOT NULL
        отдельной миграцией.
    -->
    <changeSet id="20260517-32-extend-users-for-auth" author="Abdula Basnukaev">
        <comment>ADR-040: добавляем password_hash, role, enabled, updated_at в users</comment>
        <sql>
            ALTER TABLE users
                ADD COLUMN password_hash VARCHAR(255) NULL,
                ADD COLUMN role          VARCHAR(20)  NOT NULL DEFAULT 'USER',
                ADD COLUMN enabled       BOOLEAN      NOT NULL DEFAULT TRUE,
                ADD COLUMN updated_at    TIMESTAMPTZ  NOT NULL DEFAULT now();

            ALTER TABLE users
                ADD CONSTRAINT users_role_check
                CHECK (role IN ('USER', 'ADMIN'));

            CREATE INDEX idx_users_email_lower ON users (LOWER(email));
        </sql>
        <rollback>
            <sql>
                DROP INDEX IF EXISTS idx_users_email_lower;
                ALTER TABLE users DROP CONSTRAINT IF EXISTS users_role_check;
                ALTER TABLE users
                    DROP COLUMN updated_at,
                    DROP COLUMN enabled,
                    DROP COLUMN role,
                    DROP COLUMN password_hash;
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
