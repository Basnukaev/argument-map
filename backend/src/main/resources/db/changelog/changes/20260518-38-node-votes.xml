<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Голосование за вес аргументов. 1 user может проголосовать за 1 node
        ровно 1 раз - upvote (+1) или downvote (-1). 0 не сохраняется
        отдельным значением - удалить голос = DELETE row.

        MVP - 3-point scale {-1, +1} (без сохранённого нейтрального 0).
        5-point scale {-2..+2} с категориями силы - возможное расширение
        backlog'а если возникнет необходимость более тонкой градации.

        Голоса НЕ влияют на StatusCalculation (Dung-style). Это пользовательский
        сигнал силы аргумента, ортогональный logical status STANDING/DISPUTED.
        UI агрегирует upvotes/downvotes и показывает компактным виджетом в
        NodeCard.

        Permission: только authenticated user может vote. Если он видит node
        (read access к topic'у) - может голосовать. Не нужен write access -
        vote это reaction, не модификация контента.
    -->
    <changeSet id="20260518-38-node-votes" author="Abdula Basnukaev">
        <comment>Таблица node_votes для голосования за вес аргументов</comment>
        <sql>
            CREATE TABLE node_votes (
                id        UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                node_id   UUID NOT NULL REFERENCES nodes(id) ON DELETE CASCADE,
                user_id   UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
                weight    SMALLINT NOT NULL,
                voted_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
                CONSTRAINT node_votes_weight_check CHECK (weight IN (-1, 1)),
                CONSTRAINT node_votes_unique UNIQUE (node_id, user_id)
            );

            CREATE INDEX idx_node_votes_node ON node_votes(node_id);
            CREATE INDEX idx_node_votes_user ON node_votes(user_id);
        </sql>
        <rollback>
            <sql>
                DROP INDEX IF EXISTS idx_node_votes_user;
                DROP INDEX IF EXISTS idx_node_votes_node;
                DROP TABLE IF EXISTS node_votes;
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
